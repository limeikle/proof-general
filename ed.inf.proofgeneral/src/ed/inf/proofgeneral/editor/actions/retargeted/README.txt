Note:

- these actions are new and simplified versions of the ones in ed.inf.proofgeneral.editor,
which are connected using the standard workbench mechanisms (as of 3.3) for retargetable
actions. 

Hints:

Extension uses a flag "retarget" instead of a class.

Responsibility for making actions is in 
